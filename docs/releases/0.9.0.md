# 0.9.0

- Review translated resource-page titles and content in the native DSW browser
  check, with per-field results and screenshots in the Actions artifact.
- Report source-language fallback separately from PO import and questionnaire
  switching. Fallback emits a warning; unexpected text or page-load failures
  fail verification. Partial locales remain releasable.
- Use official server 4.34.0 and client 4.34.1 in the disposable test stack.
  The Python KM parser dependency and production deployments are unchanged.
