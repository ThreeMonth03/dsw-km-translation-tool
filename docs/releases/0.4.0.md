# 0.4.0

- Compare locale coverage with a POT freshly exported by official DSW. Missing,
  untranslated, fuzzy and extra entries are reported separately from import
  validity; incomplete coverage warns without blocking partial locales.
- Verify native browser PO import, byte-for-byte download and persisted
  source/translated language switching in disposable DSW containers. Translation
  CI and release jobs retain coverage reports and real screenshots for review.
- Correct the KM update runbook: scheduled sync has no manual trigger. Run the
  alignment report manually and inspect the next scheduled sync.

The Python `dsw-models` dependency remains on official v4.27.0. Refresh downstream
tooling pins and managed docs/workflows to enable the new checks.
