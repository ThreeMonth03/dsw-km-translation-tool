# 0.3.1

- Correct the contributor guide to identify **KM Translation Operations** as
  the workflow that validates pull requests and provides PO review artifacts.
- Check the guide against the rendered workflow name in the test suite.
- Explain how to select the imported locale in project settings and compare
  the translated questionnaire with its source language in a test DSW instance.

Pin this release's full tooling commit and refresh the managed scaffold to
update downstream contribution guides. Translation content, configuration
schema, and the official `dsw-models` dependency remain unchanged.
